## 更新内容

- 修复 AgentDesk 使用自定义 Codex 地址和密钥时，因没有读取全局配置而错误请求 OpenAI 官方服务的问题。
- AgentDesk 使用独立的 `~/.codex-agentdesk` 数据目录，仅将 `config.toml` 和 `auth.json` 软链接到全局 `~/.codex`，配置修改会自动同步，会话、历史、日志和数据库仍保持隔离。
- 如果软链接无法建立，AgentDesk 会明确提示失败原因并阻止 Codex 启动，避免静默使用错误配置；已有的独立配置会先自动备份。
