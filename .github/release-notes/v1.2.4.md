## 更新内容

- Claude Code 官方安装更新改用 gh-proxy 下载 `latest/download/claude-win32-x64.zip`，解压后校验 Windows 文件、Anthropic 官方签名和版本，再原子替换到本机安装目录。
- Claude Code 更新完成、失败或回滚后清理 ZIP 和临时更新目录，避免残留大文件。
- Codex 增加本地历史全文索引：首次建立在后台分批、低速执行，后续按文件变化增量更新，并在 app-server 搜索不可用时提供本地回退。

## 验证

- 完整测试、主进程构建和 Electron 核心回归已通过。
