## 更新内容

- Codex 长会话改为分段加载，避免打开大历史记录时导致后台退出。
- 每个 Codex 会话使用独立后台，一个会话异常不再中断其他会话。
- Codex 后台异常后自动恢复当前会话，并保留草稿和待发送内容，不会自动重复任务。
- 单条超大返回只影响当前操作，其他会话可以继续使用。

## 验证

- 全部自动测试、构建和安装版回归已通过。
- 曾触发故障的多个历史会话已在开发版中成功恢复，没有再次导致软件中断。
