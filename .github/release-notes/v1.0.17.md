## 更新内容

- 新增 `/model` 命令，支持手动输入完整模型名称并切换到未出现在默认列表中的模型。
- 新增 `/compact`、`/rename` 和 `/plan` 命令，命令参数可以直接在输入框中使用。
- Provider 提供的原生命令会继续显示并原样转发，不会被 AgentDesk 的本地能力限制拦截。
- 手动输入的模型会在顶部模型选择框中显示，避免切换后看不到当前模型。
- 优化 Codex、Claude Code 和全部筛选项的选中状态显示。
